{
  "name": "zmare-stream-service",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=backend",
    "install-all": "npm install && npm install --workspace=frontend && npm install --workspace=backend"
  },
  "dependencies": {
    "youtube-dl-exec": "^3.1.5",
    "yt-dlp-exec": "^1.0.2"
  }
}
